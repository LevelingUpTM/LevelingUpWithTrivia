<UserControl x:Class="LevelingUpWithTrivia.Views.JoinRoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             d:DesignHeight="450" d:DesignWidth="800"
             mc:Ignorable="d"
             xmlns:viewmodels="clr-namespace:LevelingUpWithTrivia.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:JoinRoomViewModel}">

    <Grid Background="#001123">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">

            <TextBlock Text="Join Room" Foreground="White" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Room ID:" Foreground="White"/>
            <TextBox Text="{Binding RoomId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20" Padding="6"/>

            <Button Content="Join Room"
                    Command="{Binding JoinRoomCommand}"
                    Background="#00727C" Foreground="White" Padding="10" FontSize="16" Margin="0,0,0,10"/>

            <Button Content="Back to Main Menu"
                    Command="{Binding BackCommand}"
                    Background="#6a141a" Foreground="White" Padding="10" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>