<UserControl x:Class="LevelingUpWithTrivia.Views.LogInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelingUpWithTrivia.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="red">
        <Grid>
            <Border Background="Olive" Padding="40" CornerRadius="20" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel >
                    <TextBlock Text="Login page" FontSize="28" Foreground="White" FontWeight="Bold" TextAlignment="Center"/>

                    <!-- Username -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Username:" Width="80" VerticalAlignment="Center" Foreground="White"/>
                        <TextBox Height="30" Width="250" Background="#CCC" FontSize="14"
                        Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>

                    <!-- Password -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Password:" Width="80" VerticalAlignment="Center" Foreground="White"/>
                        <PasswordBox Height="30" Width="250" Background="#CCC"
                         PasswordChanged="PasswordBox_PasswordChanged" />
                    </StackPanel>

                    <Button Content="Login" Background="#007ACC" Foreground="White" Height="40" FontSize="16" Cursor="Hand" Command="{Binding LogInCommand}" />

                    <TextBlock Text="Don't have an account?" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    <Button Content="Sign Up" Background="#444" Foreground="White" Height="35" Cursor="Hand" Command="{Binding NavToSignupCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
