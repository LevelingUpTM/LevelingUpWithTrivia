<UserControl x:Class="LevelingUpWithTrivia.Views.SignUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelingUpWithTrivia.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Border Background="Olive" Padding="40" CornerRadius="20" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel >
                <TextBlock Text="Sign Up" FontSize="28" Foreground="White" FontWeight="Bold" TextAlignment="Center"/>

                <TextBox Height="35"/>
                <PasswordBox Height="35" />
                <PasswordBox Height="35" />

                <Button Content="Create Account" Background="#007ACC" Foreground="White" Height="40" FontSize="16" Cursor="Hand"/>

                <TextBlock Text="Already have an account?" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Button Content="Back to Login" Background="#444" Foreground="White" Height="35" Cursor="Hand" Command="{Binding NavToLoginCommand}"/>
            </StackPanel>   
        </Border>
    </Grid>
    
</UserControl>
